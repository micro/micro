apiVersion: v1
kind: Service
metadata:
  name: micro-gateway
  namespace: default
  labels:
    name: micro-gateway
    micro: runtime
spec:
  ports:
  - name: https
    port: 443
    targetPort: 443
  selector:
    name: micro-gateway
    micro: runtime
  type: LoadBalancer
